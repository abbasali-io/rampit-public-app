part of order_container_widget;

class _OrderContainerTablet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('order_container_tablet'),
    );
  }
}